"use client";

import { useTranslations } from "next-intl";

// Footer is intentionally minimal for a dashboard app — the sidebar handles navigation.
// This component is kept for layout.tsx compatibility but renders nothing visible.
export default function Footer() {
  return null;
}